import 'package:value_object_pattern/domain/value_objects/generic_string_value.dart';

class ScheduleSummaryColorValue extends GenericStringValue {
  ScheduleSummaryColorValue({
    super.defaultValue = '',
    super.isRequired = false,
  });
}
