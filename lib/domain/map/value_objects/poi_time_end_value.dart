import 'package:belluga_now/domain/value_objects/domain_optional_date_time_value.dart';

class PoiTimeEndValue extends DomainOptionalDateTimeValue {
  PoiTimeEndValue({
    super.defaultValue,
    super.isRequired = false,
  });
}
