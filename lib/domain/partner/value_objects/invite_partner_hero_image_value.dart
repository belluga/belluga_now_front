import 'package:value_object_pattern/domain/value_objects/uri_value.dart';

class InvitePartnerHeroImageValue extends URIValue {
  InvitePartnerHeroImageValue({
    super.defaultValue,
    super.isRequired = false,
  });
}
