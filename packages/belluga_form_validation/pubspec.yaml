name: belluga_form_validation
description: Reusable form validation helpers for Belluga Flutter forms.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  stream_value: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
