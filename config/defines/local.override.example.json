{
  "_comment": "Replace LANDLORD_DOMAIN with the exact browser-facing local origin for your machine. Current downstream projects may use project-owned wildcard or published hosts here, but those values must stay only in local.override.json.",
  "LANDLORD_DOMAIN": "http://tenant-local.example.test:8081"
}
