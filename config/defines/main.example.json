{
  "_comment": "Reusable main-lane example. Copy to main.json in a downstream project and replace LANDLORD_DOMAIN with the browser-facing landlord origin for that lane. Keep current project-owned domains out of the shared example default.",
  "LANDLORD_DOMAIN": "https://landlord.example.com"
}
